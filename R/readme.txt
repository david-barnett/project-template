Scripts defining functions used in this project. One or a few related functions per script?

Name the script with the same name as the single or main function in the script.

If multiple functions are defined in one script, give a self explanatory name about what sort of thing these functions do.